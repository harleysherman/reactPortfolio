/* eslint-disable no-undef */
const User = require('./User');
const Community = require('./Community');
const Achievement = require('./Post');
const Comment = require('./Comment');

module.exports = { User, Community, Achievement, Comment };
